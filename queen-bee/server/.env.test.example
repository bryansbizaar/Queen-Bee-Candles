# Test Environment Variables
# Copy this to .env in the server directory for testing

# Database Configuration (adjust these for your PostgreSQL setup)
DB_USER=bryanowens
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=queen_bee_test

# Stripe Test Keys (you can keep these as-is for testing)
STRIPE_SECRET_KEY=sk_test_your_test_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_test_key_here

# Node Environment
NODE_ENV=test
